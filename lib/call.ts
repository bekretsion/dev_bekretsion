/** Any "Talk to me" button on the site fires this; the call panel listens and opens. */
export const OPEN_CALL_EVENT = 'bk:open-call';

/** Total talk time one device gets with the assistant, across all its conversations. */
export const TALK_LIMIT_SECONDS = 4 * 60;
